# DEPRECATED — not part of the current architecture

Channel buttons are on each **desk Pi** (`deploy/desk-pi/agent/`), not a central Pico.

Kodak uses a **USB relay on the Mac Mini** (`server/hardware.js` + `KODAK_RELAY_ON` / `OFF`).

This folder is kept only as a reference experiment. Do not buy a Pico for production unless you have a new rack I/O need.
